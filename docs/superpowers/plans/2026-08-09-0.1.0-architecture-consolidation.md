# v0.1.0 架构聚拢与安全加固 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: rust-workspace, rust-module-layout, rust-concurrency

**Goal:** 将 22 个细粒度 crate 合并为 9 个聚焦 crate，重建 feature 体系，实现 Streaming 读取策略、CMap/ToUnicode 支持、WriteBackend 选择、ProcessorPipeline、4 云 OCR 引擎、MCP server、resident daemon、ISO 32000 加密签名、tracing 可观测性、cargo-fuzz、91.61% 覆盖率，最终发布 v0.1.0 到 crates.io。

**Architecture:** 9 crate 分层：easypdf（门面）/ easypdf-core（模型+IO+加密+事件）/ easypdf-derive（过程宏）/ easypdf-reader（读取+操作+streaming）/ easypdf-writer（写入+后端+字体+图片+形状）/ easypdf-markdown（Markdown 处理+OCR 集成）/ easypdf-ocr（4 云 OCR 引擎）/ easypdf-runtime（MCP+resident daemon）/ easypdf-test（集成+parity 测试）。

**Tech Stack:** Rust edition 2024, rust-version 1.88, printpdf 0.8, lopdf 0.34, ring 0.17（签名 RSA）, x509-parser（证书）, aes / cbc / sha2 / md5 / hmac（加密）, reqwest blocking（OCR HTTP）, serde_json（MCP JSON-RPC）, tracing + tracing-subscriber（可观测性）, cargo-fuzz（模糊测试）。

## Global Constraints

- 合并后每个 crate 保持单一职责，不引入循环依赖。
- feature 体系重建：修复 `ocr` feature 静默激活 `http-base` 的 bug。
- `resident` 和 `mcp` feature 移至 `easypdf-runtime`。
- 所有加密操作使用 ring 常量时间 RSA（修复 Marvin Attack / RUSTSEC-2023-0071）。
- SSRF 防护覆盖 IPv6（loopback / ULA / link-local / IPv4-mapped）。
- API key 在 Debug 输出中脱敏。
- 每个 crate 的 `lib.rs` 声明 `#![deny(unsafe_code)]`。

---

### Task 1: 22-to-9 Crate 合并

> Files:
> - Modify: `Cargo.toml`（workspace root）
> - Modify: `crates/easypdf-core/`（合并 model / io / layout / crypto / converter_registry / event / handler_chain / page_range / page_index / page_number / style / traits）
> - Modify: `crates/easypdf-reader/`（合并 reader / extract / manipulate / strategy / streaming）
> - Modify: `crates/easypdf-writer/`（合并 writer / backend / builder / font / image / shape / template）
> - Modify: `crates/easypdf-markdown/`（合并 pdf_markdown_* / processor_pipeline / processors / render / table / ocr）
> - Create: `crates/easypdf-ocr/`（http / glm / hunyuan / baidu）
> - Create: `crates/easypdf-runtime/`（mcp / resident）

**Steps:**
- [x] 设计 9-crate 分层架构（基础层/引擎层/markdown/ocr/runtime/门面）
- [x] 合并 easypdf-core -- 整合 model / io / layout / crypto / converter_registry / event / handler_chain / page_range / page_index / page_number / style / traits
- [x] 合并 easypdf-reader -- 整合 reader / extract / manipulate / strategy / streaming（byte_finder / cmap / scanner / text_extract）
- [x] 合并 easypdf-writer -- 整合 writer / backend / builder / font / image / shape / template
- [x] 合并 easypdf-markdown -- 整合 pdf_markdown_* / processor_pipeline / processors / render / table / ocr
- [x] 创建 easypdf-ocr -- 整合 http / glm / hunyuan / baidu（14 端点）
- [x] 创建 easypdf-runtime -- 整合 mcp（protocol / server / tools）/ resident（client / server / transport / tcp / unix / session / port）
- [x] 更新 easypdf 门面 -- builders / crypto_facade / facade_features / html / pdf_fill_builder / prelude / writer_helpers
- [x] 更新 workspace Cargo.toml -- 9 个 members
- [x] 验证 `cargo check` 全 workspace 通过

---

### Task 2: Feature 体系重建

> Files:
> - Modify: `crates/easypdf/Cargo.toml`
> - Modify: 各 crate `Cargo.toml`

**Steps:**
- [x] 修复 `ocr` feature 不再静默激活 `http-base`
- [x] `markdown-table` / `render` / `ocr` 启用 `easypdf-markdown` 内子模块
- [x] `resident` 和 `mcp` feature 移至 `easypdf-runtime`
- [x] 验证 feature 组合测试通过

---

### Task 3: Streaming ReadStrategy

> Files:
> - Modify: `crates/easypdf-reader/src/strategy.rs`
> - Create: `crates/easypdf-reader/src/streaming/`（byte_finder.rs / cmap.rs / scanner.rs / text_extract.rs）

**Steps:**
- [x] 实现 `ReadStrategy::Streaming` -- 字节流扫描，不构建完整 `Document` 对象
- [x] 实现 `ReadStrategy::auto()` -- 按文件大小自动选择（Full < 5MB / Lazy 5-100MB / Streaming > 100MB）
- [x] 实现 streaming 模块：byte_finder / cmap / scanner / text_extract
- [x] 验证 streaming 测试通过

---

### Task 4: CMap / ToUnicode 支持

> Files:
> - Create: `crates/easypdf-reader/src/streaming/cmap.rs`

**Steps:**
- [x] 实现 CMap 解析 -- 解码 CMap 编码的字体映射
- [x] 实现 ToUnicode 表读取 -- 修复 CJK 文本提取乱码
- [x] 验证 CJK 文本提取测试通过

---

### Task 5: WriteBackend 选择

> Files:
> - Create: `crates/easypdf-writer/src/backend.rs`

**Steps:**
- [x] 实现 `WriteBackend` 枚举 -- InMemory（默认）/ Spill（页面级临时文件，常量内存）/ Auto（阈值自动选择）
- [x] 实现 `PageSpillWriter` -- 页面内容序列化到临时文件
- [x] 验证 WriteBackend 测试通过

---

### Task 6: PdfWriterBuilder + WriteHandlerChain

> Files:
> - Create: `crates/easypdf-core/src/handler_chain.rs`
> - Modify: `crates/easypdf-writer/src/writer.rs`

**Steps:**
- [x] 实现 `WriteHandlerChain` -- 优先级排序的稳定执行管道
- [x] 实现 `PdfWriterBuilder` -- 链式配置 writer
- [x] 验证 handler chain 测试通过

---

### Task 7: ConverterRegistry

> Files:
> - Create: `crates/easypdf-core/src/converter_registry.rs`

**Steps:**
- [x] 实现 `ConverterRegistry` -- 类型擦除的双向转换器注册
- [x] 实现 `with_defaults()` -- 预填充默认转换器
- [x] 验证转换器注册测试通过

---

### Task 8: 4 Cloud OCR Engines

> Files:
> - Create: `crates/easypdf-ocr/src/`（http/ / glm/ / hunyuan/ / baidu/）

**Steps:**
- [x] 实现 `HttpOcrEngine` 统一抽象 trait（reqwest blocking HTTP / base64 图片编码 / 结构化错误）
- [x] 实现 GLM-OCR（智谱 BigModel，feature-gated `ocr-glm`）
- [x] 实现 HunyuanOCR（腾讯云，TC3-HMAC-SHA256 签名，feature-gated `ocr-hunyuan`）
- [x] 实现 Baidu Qianfan / PP-OCRv6（14 API 端点 + OAuth token 管理，feature-gated `ocr-baidu`）
- [x] 实现 DeepSeek-OCR-2（OpenAI 兼容协议，feature-gated `ocr-deepseek`）
- [x] 实现限流（`RateLimitConfig`）和重试（`BackoffStrategy`）
- [x] 实现 SSRF 防护（URL 校验，IPv6 覆盖）
- [x] 实现 API key Debug 脱敏
- [x] 验证 OCR 引擎测试通过

---

### Task 9: Resident Daemon

> Files:
> - Create: `crates/easypdf-runtime/src/resident/`（server.rs / session.rs / client.rs / port.rs / transport/）

**Steps:**
- [x] 实现 `Transport` trait -- Unix socket（默认）+ Windows TCP fallback
- [x] 实现 `UnixTransport` -- Unix domain socket 绑定和监听
- [x] 实现 `TcpTransport` -- Windows TCP fallback
- [x] 实现 `ResidentServer` -- 会话管理、端口分配
- [x] 实现自适应 autosave（EMA 平滑，空闲超时 watchdog）
- [x] 验证 resident daemon 测试通过

---

### Task 10: MCP Server

> Files:
> - Create: `crates/easypdf-runtime/src/mcp/`（protocol.rs / server.rs / tools.rs）

**Steps:**
- [x] 实现 MCP 协议（stdio JSON-RPC）
- [x] 实现 7 个工具：pdf_read_text / pdf_to_markdown / pdf_create_text / pdf_merge / pdf_split / pdf_metadata / pdf_page_count
- [x] 实现 `McpServer` 主循环
- [x] 验证 MCP server 测试通过

---

### Task 11: PdfBlock IR 扩展

> Files:
> - Modify: `crates/easypdf-core/src/model/pdf_block.rs`
> - Modify: `crates/easypdf-core/src/model/pdf_block_type.rs`

**Steps:**
- [x] 扩展 `PdfBlock` 从 5 到 14 个变体 -- 新增 Code / Formula / PageBreak / Footnote / TableCell / BlockQuote / HorizontalRule / Link / Unknown
- [x] 更新 `PdfBlockType` 枚举
- [x] 验证 PdfBlock 测试通过

---

### Task 12: ProcessorPipeline

> Files:
> - Create: `crates/easypdf-markdown/src/processor_pipeline.rs`
> - Create: `crates/easypdf-markdown/src/processors/`

**Steps:**
- [x] 实现 `ProcessorPipeline` -- 能力协商和优先级排序
- [x] 实现 heading_detector / link_extractor / reading_order 等处理器
- [x] 实现 table detector / heuristic / parser
- [x] 验证 ProcessorPipeline 测试通过

---

### Task 13: ISO 32000 加密签名

> Files:
> - Modify: `crates/easypdf-core/src/crypto/`（encrypt.rs / sign.rs / sign_cms.rs / sign_der.rs / sign_pdf.rs）

**Steps:**
- [x] 实现 AES-128（V4/R4）加密/解密
- [x] 实现 AES-256（V5/R6）加密/解密（ISO 32000-2）
- [x] 实现 PKCS#7/CMS detached SignedData（ring RSA + x509-parser）
- [x] 实现 permission flags（PRINT / MODIFY / COPY / FILL_FORMS 等）
- [x] ring 常量时间 RSA 迁移（修复 Marvin Attack / RUSTSEC-2023-0071）
- [x] 修复双重哈希签名问题（签名前不再先哈希，符合 CMS 规范）
- [x] 验证加密/签名 roundtrip 测试通过

---

### Task 14: Tracing 可观测性

> Files:
> - Create: `crates/easypdf-core/src/logging.rs`
> - Modify: 各 crate `Cargo.toml`（tracing 依赖）

**Steps:**
- [x] 集成 tracing + tracing-subscriber（env-filter + JSON 输出）
- [x] 在 reader / writer / markdown / IPC 模块添加结构化 span
- [x] 实现 `easypdf_core::logging` 模块（init_compact / init_json）
- [x] 验证 tracing 集成测试通过

---

### Task 15: Security Fixes

> Files:
> - Modify: `crates/easypdf-core/src/crypto/`（rsa → ring 迁移）
> - Modify: `crates/easypdf-core/src/io/ssrf_guard.rs`（IPv6 覆盖）
> - Modify: `crates/easypdf-ocr/src/http/auth.rs`（API key 脱敏）

**Steps:**
- [x] RUSTSEC-2023-0071 修复 -- rsa → ring 迁移（生产代码路径）
- [x] RUSTSEC-2025-0055 修复 -- tracing-subscriber >= 0.3.20
- [x] 解压炸弹防护修复 -- 移除 64KB 豁免，检查绝对解压大小
- [x] SSRF IPv6 防护 -- loopback / ULA / link-local / IPv4-mapped 全覆盖
- [x] API key Debug 脱敏 -- GlmConfig / BaiduConfig 等结构体
- [x] 验证安全测试通过

---

### Task 16: Cargo Fuzz

> Files:
> - Create: `fuzz/fuzz_targets/`（pdf_parse.rs / streaming_scan.rs / pdf_encrypt_decrypt.rs / pdf_sign_verify.rs / markdown_convert.rs / ssrf_url.rs）

**Steps:**
- [x] 创建 6 个 fuzz target：pdf_parse / streaming_scan / pdf_encrypt_decrypt / pdf_sign_verify / markdown_convert / ssrf_url
- [x] 生成初始语料库
- [x] 修复 fuzz 发现的 byte_finder OOB panic
- [x] 验证 fuzz target 编译和运行

---

### Task 17: 测试覆盖率

> Files:
> - Modify: 各 crate tests/

**Steps:**
- [x] 达到 1522 个测试通过
- [x] 达到 91.61% 行覆盖率
- [x] clippy 零警告（workspace 级 `similar_names = "allow"` 避免 PDF 对象名误报）
- [x] rustdoc 零警告

---

### Task 18: 发布准备

> Files:
> - Modify: 各 crate `Cargo.toml`（版本号）
> - Create: `CHANGELOG.md`
> - Create: `docs/RELEASE_LOG_0.1.0.md`
> - Create: `docs/RELEASE_CHECKLIST.md`

**Steps:**
- [x] 版本统一为 0.1.0（首次发布）
- [x] 发布 8 个 crate 到 crates.io（easypdf-test 不发布）
- [x] 编写 CHANGELOG.md（Added / Security / Changed 分类）
- [x] 编写双语文档（14 个文档文件，中英双语 README / 架构文档 / 使用指南）
- [x] 编写 RELEASE_LOG_0.1.0.md
- [x] 编写 RELEASE_CHECKLIST.md
- [x] 同步 roadmap.md

---

## Acceptance / Verification

```bash
cargo test --workspace                    # 1522 tests pass
cargo clippy --workspace -- -D warnings   # 0 warnings
cargo fmt --check                         # 100% compliant
cargo audit                               # 0 CVE
cargo fuzz list                           # 6 targets
cargo publish --dry-run                   # 8 crates ready
```

## 关键发现（代码核对）

- roadmap 0.1 标注 "22-crate-to-9-crate" 在 Foundation 阶段，但实际合并发生在 v0.2 架构聚拢阶段。
- DeepSeek OCR 引擎没有独立模块目录，而是通过 `HttpOcrEngine` 的 OpenAI 兼容协议实现。
- `#![deny(unsafe_code)]` 而非 `#![forbid(unsafe_code)]`，所有 7 个 crate（不含 easypdf-derive）均为 deny。
- Baidu OCR 确认 14 个 API 端点（`crates/easypdf-ocr/src/baidu/config.rs:61`）。
- Resident autosave 使用 EMA 平滑（`crates/easypdf-runtime/src/resident/session.rs:34`）。
- MCP 确认 7 个工具（`crates/easypdf-runtime/src/mcp/tools.rs`）。
- fuzz 发现并修复 byte_finder OOB panic。

## 依赖关系

```
Task 1 (22→9 合并)
    │
    ├── Task 2 (Feature 重建)
    │
    ├── Task 3 (Streaming) ──→ Task 4 (CMap)
    │
    ├── Task 5 (WriteBackend)
    ├── Task 6 (WriterBuilder + HandlerChain)
    ├── Task 7 (ConverterRegistry)
    │
    ├── Task 8 (OCR) ──→ Task 15 (Security, SSRF + API key)
    │
    ├── Task 9 (Resident) ──→ Task 10 (MCP)
    │
    ├── Task 11 (PdfBlock 扩展)
    ├── Task 12 (ProcessorPipeline)
    │
    ├── Task 13 (加密签名) ──→ Task 15 (Security, ring 迁移)
    │
    ├── Task 14 (Tracing)
    │
    └── Task 16 (Fuzz) ──→ Task 17 (测试覆盖率) ──→ Task 18 (发布)
```
