# v1.0.0 Stable 发布门禁 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: rust-testing, rust-code-review, rust-semver

**Goal:** 达到 1.0.0 stable 发布门禁：semver 保证、Windows MSRV 测试、property-based testing、完整迁移指南，使 easypdf-rust 成为生产可用的 PDF 库。

**Architecture:** 延续 9-crate 架构。主要修改 CI 矩阵（新增 Windows）、测试基础设施（proptest）、文档（迁移指南）。

**Tech Stack:** Rust 1.88+, cargo-semver-checks, proptest, criterion

## Global Constraints

- 保持 `#![deny(unsafe_code)]` 策略。
- 不破坏现有 1900+ 测试。
- cargo-semver-checks 零违反。
- Windows CI 通过（windows-latest + rust 1.88）。
- 属性测试覆盖率 > 80% 公共 API。

---

### Task 1: Semver Guarantees

> Files:
> - Modify: `.github/workflows/ci.yml`
> - Create: `docs/API_STABILITY.md`

**Steps:**
- [ ] 集成 cargo-semver-checks 到 CI
- [ ] 编写 API 稳定性承诺文档
- [ ] 定义 semver 版本策略（0.2.x → 0.3.x → 1.0）
- [ ] 添加 2 个测试：semver-checks 通过 / API 签名不变

---

### Task 2: Windows MSRV Testing

> Files:
> - Modify: `.github/workflows/ci.yml`

**Steps:**
- [ ] 在 CI 中添加 Windows 矩阵（windows-latest + rust 1.88）
- [ ] 修复 Windows 特有问题（路径分隔符 / 文件锁定 / TCP transport）
- [ ] 验证 Windows CI 通过
- [ ] 添加 3 个测试：路径分隔符 / 文件锁定 / TCP transport

---

### Task 3: Property-based Testing -- 加密签名

> Files:
> - Create: `crates/easypdf-core/tests/proptest_crypto.rs`

**Steps:**
- [ ] 添加 proptest 依赖
- [ ] 为加密/签名添加属性测试 -- 任意密钥/明文 roundtrip
- [ ] 为权限标志添加属性测试 -- 任意权限组合 roundtrip
- [ ] 添加 8 个属性测试（2048 adversarial cases each）

---

### Task 4: Property-based Testing -- PDF 解析

> Files:
> - Create: `crates/easypdf-reader/tests/proptest_parse.rs`

**Steps:**
- [ ] 为 PDF 解析添加属性测试 -- 任意字节流不 panic
- [ ] 为 ReadStrategy 添加属性测试 -- 任意文件大小选择正确
- [ ] 为 CMap 解析添加属性测试 -- 任意 CMap 数据不 panic
- [ ] 添加 6 个属性测试

---

### Task 5: Property-based Testing -- Markdown 转换

> Files:
> - Create: `crates/easypdf-markdown/tests/proptest_markdown.rs`

**Steps:**
- [ ] 为 Markdown 转换添加属性测试 -- 任意 Markdown 不 panic
- [ ] 为 ProcessorPipeline 添加属性测试 -- 任意处理器组合不 panic
- [ ] 添加 4 个属性测试

---

### Task 6: Property-based Testing -- 写入引擎

> Files:
> - Create: `crates/easypdf-writer/tests/proptest_write.rs`

**Steps:**
- [ ] 为写入引擎添加属性测试 -- 任意 PdfBlock 不 panic
- [ ] 为表格渲染添加属性测试 -- 任意表格结构不 panic
- [ ] 添加 4 个属性测试

---

### Task 7: Complete Migration Guide

> Files:
> - Create: `docs/MIGRATION_GUIDE.md`

**Steps:**
- [ ] 编写从 0.1.x 到 1.0 的迁移指南
- [ ] 记录所有 breaking changes（按版本分组）
- [ ] 提供迁移示例代码
- [ ] 提供迁移检查清单

---

### Task 8: 测试覆盖率提升

> Files:
> - Modify: 各 crate tests/

**Steps:**
- [ ] 当前 1900+ 测试，目标 2000+
- [ ] 补充 proptest 属性测试
- [ ] 补充 Windows 特有测试
- [ ] 验证 clippy / rustfmt 零警告

---

### Task 9: 文档完善

> Files:
> - Modify: `docs/usage-guide.md`
> - Modify: `docs/roadmap.md`
> - Modify: `README.md` + `README_zh.md`

**Steps:**
- [ ] 更新使用指南：新增 1.0 特性说明
- [ ] 更新路线图：标记 1.0 Stable 完成
- [ ] 更新 README：新增 1.0 稳定性承诺
- [ ] 编写 v1.0.0 发布说明

---

### Task 10: 发布

> Files:
> - Modify: `CHANGELOG.md`
> - Modify: 各 crate `Cargo.toml`

**Steps:**
- [ ] bump version to 1.0.0
- [ ] 发布 8 个 crate 到 crates.io
- [ ] 创建 git tag v1.0.0
- [ ] 更新 RELEASE_LOG

---

## Acceptance / Verification

```bash
cargo test --workspace                    # 2000+ tests pass
cargo clippy --workspace -- -D warnings   # 0 warnings
cargo fmt --check                         # 100% compliant
cargo semver-checks                       # 零违反
cargo test --target x86_64-pc-windows-msvc # Windows 通过
cargo test --features proptest            # 属性测试通过
```

## 关键发现（代码核对）

- 当前无 Windows CI 矩阵（仅 Linux + macOS）。
- 当前无 proptest 属性测试。
- 当前无 cargo-semver-checks 集成。
- 当前无迁移指南。
- 公共 API 已发布到 crates.io（v0.1.0），但无 semver 保证文档。

## 依赖关系

```
Task 1 (Semver)   ──→ Task 7 (迁移指南)
Task 2 (Windows)  ──→ Task 8 (测试)
Task 3 (proptest 加密) ──→ Task 8 (测试)
Task 4 (proptest 解析) ──→ Task 8 (测试)
Task 5 (proptest MD)   ──→ Task 8 (测试)
Task 6 (proptest 写入) ──→ Task 8 (测试)
Task 7 (迁移指南) ──→ Task 9 (文档)
Task 8 (测试)     ──→ Task 10 (发布)
Task 9 (文档)     ──→ Task 10 (发布)
```
