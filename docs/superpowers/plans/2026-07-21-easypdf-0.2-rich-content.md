# easypdf-rust v0.2 Rich Content -- F1-F16 + C1/C2 Full Feature Implementation

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 基于 `docs/implementation-plan.md` 的 F1-F16 功能编号和 C1/C2 架构改进，实现表格渲染、图片插入、自定义字体、矢量图形、页眉页脚、多页写入器、水印、PDF 图层、加密、权限、PDF/A、XMP、数字签名、HTML→PDF、SVG→PDF、Markdown→PDF 等全部功能。

**Architecture:** 复用 Phase 1 的 crate 分层。加密/签名逻辑放在 `easypdf-core::crypto` 模块（后在 v0.2 架构聚拢时合并）。HTML→PDF 通过 printpdf 的 `from_html()` 实现（需 Chromium）。SVG 通过 printpdf 的 `Svg::parse()` 实现。

**Tech Stack:** printpdf 0.8（图片/字体/形状/HTML/SVG）, lopdf 0.34（水印注入/图层）, aes crate（加密）, ring 0.17（签名 RSA）, x509-parser（证书解析）。

## Global Constraints

- 加密实现必须符合 ISO 32000 规范（AES-128 V4/R4 / AES-256 V5/R6）。
- 签名使用 PKCS#7/CMS detached SignedData，RSA-PKCS#1v1.5 + SHA-256。
- HTML→PDF 功能通过 feature gate 控制（需系统 Chromium）。
- 每个功能有独立测试，不依赖外部服务。

---

### F1. Tables 表格渲染

- [x] **Step 1:** 扩展 `PdfTable` -- 添加 `column_widths` 自动计算、`row_height` 默认值
- [x] **Step 2:** 新增 `TableRenderConfig` -- 边框样式映射到 printpdf Op
- [x] **Step 3:** 实现 `PdfWriter::write_table()` -- 遍历 rows/columns，画线+写文本
- [x] **Step 4:** 新增 `PdfTableBuilder<T>` Builder -- headers_from + data + position
- [x] **Step 5:** 验证表格渲染测试通过

### F2. Images 图片

- [x] **Step 1:** 实现 `PdfWriter::write_image(&self, img, x, y, w, h)` -- 调用 `RawImage::decode_from_bytes`
- [x] **Step 2:** `PdfImage` 添加 `from_path(path)` / `from_bytes(bytes, format)` 构造函数
- [x] **Step 3:** 新增 `PdfImageBuilder` -- `.add_image(path)` → `.position(x, y)` → `.size(w, h)`
- [x] **Step 4:** 验证图片插入测试通过

### F3. Custom Fonts 自定义字体 (TTF/OTF)

- [x] **Step 1:** 实现 `PdfWriter::register_font_from_path(path)` -- 读文件 → `ParsedFont::from_bytes` → `doc.add_font`
- [x] **Step 2:** 实现 `PdfWriter::register_font_from_bytes(key, font_data)` -- 从内存注册
- [x] **Step 3:** 实现 `PdfWriter::write_text_with_custom_font(text, font_key, size, x, y)`
- [x] **Step 4:** 修改 `FontFamily` -- 支持 `Custom(Cow<'static, str>)` 变体
- [x] **Step 5:** 验证自定义字体测试通过

### F4. Shapes 矢量图形

- [x] **Step 1:** 实现 `draw_line(x1, y1, x2, y2, line_width)` -- 构建 `Line` → `Op::DrawLine`
- [x] **Step 2:** 实现 `draw_rect_stroke(x, y, w, h, line_width)` -- `Rect::to_line()` + `DrawLine`
- [x] **Step 3:** 实现 `draw_circle(cx, cy, radius, line_width)` -- 4 段三次贝塞尔曲线近似（k = 0.5522847498）
- [x] **Step 4:** 验证矢量图形测试通过

### F5. Headers & Footers 页眉页脚

- [x] **Step 1:** 实现 `PageNumberHandler` / `TextHeaderHandler` / `TextFooterHandler` 内置处理器
- [x] **Step 2:** `PdfWriteHandler` 的 `after_page` 回调注入页眉/页脚
- [x] **Step 3:** 验证页眉页脚测试通过

### F6. Multi-page Writer 多页写入器

- [x] **Step 1:** 维护 `Vec<PdfPage>` 累积所有页面
- [x] **Step 2:** `add_page()` 中将当前 ops 转为 PdfPage 推入 `pages`
- [x] **Step 3:** `finish()` 中调用 `doc.with_pages(pages)`
- [x] **Step 4:** 新增 `current_page_number()` 方法
- [x] **Step 5:** 验证多页写入测试通过

### F7. Watermarks 水印

- [x] **Step 1:** 实现文本水印 -- 遍历页面，构建 BT/ET 文本块（半透明、居中、旋转 45 度）
- [x] **Step 2:** 实现图片水印 -- 注入 XObject，在内容流中 `Do` 引用
- [x] **Step 3:** 实现 `PdfWatermarkBuilder` -- opacity / rotation / color 配置
- [x] **Step 4:** 验证水印测试通过

### F8. PDF Layers (OCG) PDF 图层

- [ ] **Step 1:** 实现 OCG 字典创建（/OCG、/OCProperties、/D）
- [ ] **Step 2:** 对每页内容流用 /OC 标记包裹
- [ ] **Step 3:** 提供 `add_layer(name)` → layer ID → `draw_on_layer(layer_id, ...)`

> **注意**: 此功能用户需求量低、实现复杂度高，已推迟至 v1.0 后评估。

### F9. Encryption 加密

- [x] **Step 1:** 实现 AES-128-CBC 加密（V4/R4）-- 128 位文件加密密钥
- [x] **Step 2:** 实现 AES-256-CBC 加密（V5/R6）-- 256 位文件加密密钥（ISO 32000-2）
- [x] **Step 3:** 实现 `PdfEncryption` Builder -- user_password / owner_password / algorithm / permissions
- [x] **Step 4:** 实现 `encrypt_pdf()` / `decrypt_pdf()` 函数
- [x] **Step 5:** 实现 `EasyPdf::encrypt(password)` 门面方法
- [x] **Step 6:** 验证加密/解密测试通过

### F10. Permission Flags 权限标志

- [x] **Step 1:** 定义 `PdfPermissions` -- PRINT / MODIFY / COPY / FILL_FORMS / EXTRACT / ASSEMBLE / PRINT_HIGH
- [x] **Step 2:** 加密时设置 `/P` 权限位掩码
- [x] **Step 3:** Builder 方法 `allow_printing()` / `deny_modification()` 等
- [x] **Step 4:** 验证权限测试通过

### F11. PDF/A Validation PDF/A 校验

- [ ] **Step 1:** 实现 PDF/A-1b 校验（禁止加密、禁止 LZW、强制 XMP、强制嵌入字体）
- [ ] **Step 2:** 实现 PDF/A-2b 校验
- [ ] **Step 3:** 实现 PDF/A-3b 校验

> **注意**: 代码中已有基础 `validate_pdfa()` 方法（检查加密和 XMP），但完整校验未实现。

### F12. XMP Metadata XMP 元数据

- [ ] **Step 1:** 扩展 `PdfMetadata` -- 添加 `create_date` / `modify_date` / `document_id` 等 XMP 字段
- [ ] **Step 2:** 生成 XMP XML 字符串，作为 PDF stream 对象添加到 document catalog
- [ ] **Step 3:** 验证 XMP 元数据测试通过

> **注意**: XMP 元数据生成尚未实现，roadmap 标注为 0.5 Compliance 阶段。

### F13. Digital Signatures 数字签名

- [x] **Step 1:** 实现 PKCS#7/CMS detached SignedData -- RSA-PKCS#1v1.5 + SHA-256
- [x] **Step 2:** 使用 ring 0.17 常量时间 RSA（修复 Marvin Attack / RUSTSEC-2023-0071）
- [x] **Step 3:** 使用 x509-parser 解析 X.509 证书
- [x] **Step 4:** 实现 `PdfSigner` Builder -- certificate / private_key / reason / location
- [x] **Step 5:** 实现 `sign_pdf()` / `verify_pdf_signature()` 函数
- [x] **Step 6:** 实现 `EasyPdf::sign(signer)` 门面方法
- [x] **Step 7:** 验证签名/验证测试通过

### F14. HTML → PDF

- [x] **Step 1:** 实现 `EasyPdf::from_html(html)` -- 调用 `printpdf::PdfDocument::from_html()`
- [x] **Step 2:** 实现 `HtmlToPdfBuilder` -- page_size / margins / chromium_path 配置
- [x] **Step 3:** feature gate: `html` feature，默认不启用（需系统 Chromium）
- [x] **Step 4:** 验证 HTML→PDF 测试通过

### F15. SVG → PDF

- [ ] **Step 1:** 调用 `Svg::parse(svg_str)` → `doc.add_xobject` → `Op::UseXobject`
- [ ] **Step 2:** 新增 `add_svg()` / `add_svg_file()` Builder 方法
- [ ] **Step 3:** 验证 SVG→PDF 测试通过

> **注意**: SVG→PDF 尚未实现，roadmap 标注为 0.6 Converters 阶段待做。

### F16. Markdown → PDF

- [x] **Step 1:** 实现 `EasyPdf::from_markdown(md)` -- Markdown → HTML → PDF 两阶段转换
- [x] **Step 2:** 使用 pulldown-cmark 转 HTML，再调用 `PdfDocument::from_html()`
- [x] **Step 3:** 验证 Markdown→PDF 测试通过

### C1. Engine Abstraction Layer 引擎抽象层

- [ ] **Step 1:** 定义 `PdfEngine` trait（create_writer / create_reader / create_manipulator）
- [ ] **Step 2:** 实现 feature flags（engine-lopdf / engine-printpdf）

> **注意**: 当前只有一组有效引擎（lopdf 读/printpdf 写），抽象层已推迟。

### C2. Layout Engine 自动布局引擎

- [x] **Step 1:** 定义 `FlowLayout` -- direction / margins / spacing / cursor
- [x] **Step 2:** 实现 `LayoutSink` trait -- 后端无关布局抽象
- [x] **Step 3:** 实现 `next_position()` / `remaining_space()` / `new_page()`
- [x] **Step 4:** 验证布局引擎测试通过

---

## 状态核对说明

核对依据：
- `easypdf/src/writer_helpers.rs:66` -- `pub fn write_table()` 存在
- `easypdf/src/builders.rs:95` -- `pub fn add_table()` 存在
- `easypdf-writer/src/image.rs:15` -- `pub fn write_image()` 存在
- `easypdf-writer/src/writer.rs:196` -- `pub fn register_font_from_path()` 存在
- `easypdf-writer/src/writer.rs:202` -- `pub fn register_font_from_bytes()` 存在
- `easypdf-writer/src/shape.rs:8` -- `pub fn draw_line()` 存在
- `easypdf-writer/src/shape.rs:35` -- `pub fn draw_rect_stroke()` 存在
- `easypdf-writer/src/shape.rs:58` -- `pub fn draw_circle()` 存在
- `easypdf-core/src/crypto/encrypt.rs` -- AES-128/256 加密实现
- `easypdf-core/src/crypto/sign.rs` -- PKCS#7/CMS 签名实现（ring + x509-parser）
- `easypdf-core/src/crypto/sign_pdf.rs` -- `sign_pdf()` / `verify_pdf_signature()` 函数
- `easypdf/src/crypto_facade.rs:21` -- `pub fn encrypt()` 门面方法
- `easypdf/src/crypto_facade.rs:48` -- `pub fn sign()` 门面方法
- `easypdf/src/lib.rs:202` -- `pub fn from_html()` 存在
- `easypdf/src/lib.rs:214` -- `pub fn from_markdown()` 存在
- `easypdf-writer/src/writer.rs:65` -- `pages: Vec<PdfPage>` 多页支持
- `easypdf-writer/src/backend.rs:59` -- `pub enum WriteBackend` (InMemory / Spill / Auto)

与 roadmap 出入：
- **F8 PDF Layers**: 代码中未找到 OCG 实现，roadmap 也承认推迟。标 `[ ]` 正确。
- **F11 PDF/A**: `easypdf-reader/src/manipulate.rs:260` 有 `validate_pdfa()` 基础方法（检查加密和 XMP），但完整 PDF/A-1b/2b/3b 校验未实现。roadmap 0.5 标 `[ ]` 正确。
- **F12 XMP Metadata**: 未找到 XMP 生成代码。roadmap 0.5 标 `[ ]` 正确。
- **F15 SVG→PDF**: 未找到 `Svg::parse` 或 `add_svg` 调用。roadmap 0.6 标 `[ ]` 正确。
- **C1 Engine Abstraction**: 未找到 `PdfEngine` trait。已推迟，标 `[ ]` 正确。
